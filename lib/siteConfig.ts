/**
 * Whether Statline charges for building a profile. Set to 0 to keep the
 * platform free (the default) — the pricing gate is skipped entirely and
 * profile generation proceeds right after the age gate. Set to a value
 * greater than 0 to require payment confirmation first. This constant is
 * the only thing that needs to change to turn paid access on or off.
 */
export const SITE_PRICE = 0.0;
